{
    "name": "telegram_test",
    "version": "1.0.0",
    "description": "telegram test",
    "main": "src/index.ts",
    "scripts": {
        "dev": "ts-node --files src/index.ts",
        "nodemon": "nodemon",
        "start": "npm run prebuild && npm run build && node dist/index.js",
        "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
        "build": "tsc -p ."
    },
    "author": "burmistrovdm",
    "license": "ISC",
    "dependencies": {
        "dotenv": "^8.2.0",
        "telegraf": "^3.38.0"
    },
    "devDependencies": {
        "@types/node": "^13.13.2",
        "nodemon": "^2.0.4",
        "ts-node": "^8.10.2",
        "tslint": "^6.1.1",
        "typescript": "^3.8.3"
    }
}
